# be-backend
dev:be-backend-deploy:
  tags:
    - idc-runner
  stage: deploy
  when: manual
  needs:
    - dev:be-backend-build
  only:
    refs:
      - branches
    changes:
      - be-backend/**/*
      - be-common/**/*
      - be-parent/**/*
    variables:
      - $CI_COMMIT_REF_NAME =~ /^test/
  environment:
    name: dev
  variables:
    SSH_IP: 10.1.115.57
    SSH_PORT: 65022
    SSH_NAME: 7moorsuper
    mavenProfile: devwh
  script:
    - chmod +x $PWD/scripts/runner_deploy.sh
    - sh -c "$PWD/scripts/runner_deploy.sh $SSH_IP $SSH_PORT $SSH_NAME $PWD/ci/dev/common-vars.sh"
